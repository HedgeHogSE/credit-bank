spring:
  application:
    name: deal
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/deal_db}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:Sashaezhak2006}

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.yaml

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

server:
  port: 8081
